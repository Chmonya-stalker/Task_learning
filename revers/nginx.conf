user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
     server {
	  listen 80;

	  location / {
          root /usr/share/nginx/html ;
			  }
	  location /zoo/ {
	  proxy_set_header Host $host;
    	  proxy_set_header X-Real-IP $remote_addr;
          proxy_pass http://flask:80/zoo/ ; # Указание порта здесь лишнее
					    # Кроме того, насколько тут необходим редирект с маршутом /zoo ? 
					    # нельзя просто proxy_pass http://flask; ?
			  }

	  location /speed/ {
	  proxy_set_header Host $host;
    	  proxy_set_header X-Real-IP $remote_addr;
          proxy_pass http://testspeed:80/ ; # Указание порта здесь лишнее 
			  }


	     }
}
