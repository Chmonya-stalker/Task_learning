user  nginx;
worker_processes  auto;

error_log  /var/log/nginx/error.log notice;
pid        /var/run/nginx.pid;


events {
    worker_connections  1024;
}


http {
     server {
	  listen 80;

	  location / {
          root /usr/share/nginx/html ;
			  }
	  location /flask {
          proxy_pass http://flask:5000/ ;
			  }

	  location /speed {
          proxy_pass http://testspeed:80/ ;
			  }


	     }
}
